package database
//Accessing information about devices
import (
	/*"errors"
	log "github.com/Sirupsen/logrus"*/
)

func AllDeviceStatus() {
	//TODO: This is the function to be run when a user wants to list node status..
	/*db, _ := NewConnection()
	defer db.Close()

	rows, err := db.Query("SELECT * FROM devices")*/
}
